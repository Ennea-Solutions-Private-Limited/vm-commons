package com.ennea.enneaservices.model.whatsapp;

public interface WhatsappTemplateParameter {
    enum Type {
        currency,
        date_time,
        document,
        image,
        payload,
        text,
        video
    }
}
